{
  "name": "nexa",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @nexa/api db:generate",
    "db:migrate": "pnpm --filter @nexa/api db:migrate",
    "db:push": "pnpm --filter @nexa/api db:push",
    "db:studio": "pnpm --filter @nexa/api db:studio"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
